[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm run typecheck:libs && pnpm --filter @workspace/aivec run build"]

[start]
cmd = "pnpm --filter @workspace/aivec run serve"
